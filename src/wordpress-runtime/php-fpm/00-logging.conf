; Global PHP-FPM logging settings
; These affect the master process and are defaults for pools

error_log = /dev/stderr ; Log errors to stderr for Docker/Cloud Run compatibility
log_level = notice           ; Default log level
